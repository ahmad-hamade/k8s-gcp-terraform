apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nginxdemos-hello.fullname" . }}-test-connection"
  labels:
    {{- include "nginxdemos-hello.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nginxdemos-hello.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
